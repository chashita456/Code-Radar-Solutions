#include<stdio.h>
int main(){
    char a;
    scanf("%c",a);
    if(a=='a' || a=='e' || a=='i' || a=='o' || a=='u' || a=='A' || a=='E' || a=='I' || a=='0' || a=='U'){
        printf("vowel");
    }
    else if(a=='b' || a=='c' || a=='d' || a=='f' || a=='g' || a=='h'|| a=='j'||a=='k'||a=='l'|| )
}
