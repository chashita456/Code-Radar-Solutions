#include<stdio.h>
#include<string.h>
int main(){
    char str[100];
    char str1[100];
    scanf("%s",str);
    scanf("%s",str1);
    int flag =0;
    for(int i=0;str[i]!='\0';i++){
        if(str1==str[len-1]){
            flag =1;  
        }
        
        }
        if(flag==1)
    }

