#include<stdio.h>
#include<string.h>
int main(){
    char str[100];
    int len =0;
    fgets(str,sizeof(str),stdin);
    len = strlen(str);
    for(int i=0;i<len;i++){
        for(int j=i+1;j<len;j++){
            if(i=)
        }

    }
}
